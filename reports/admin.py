"""
reports/admin.py

The reports app has no models of its own — it is a read-only analytics
layer built on top of data from accounts, books, members and transactions.

No admin registrations needed; this file is kept as a placeholder so
Django's app registry is satisfied.
"""
